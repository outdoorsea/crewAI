# Core pipeline dependencies (Python 3.13 compatible)
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0,<3.0.0

# CrewAI and LLM dependencies (compatible versions)
crewai>=0.11.0,<0.12.0
langchain>=0.1.0,<0.2.0
langchain-community>=0.0.20,<0.1.0

# Myndy AI dependencies
sentence-transformers>=2.2.2
qdrant-client>=1.6.0,<2.0.0

# Utility dependencies
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Python 3.13 compatibility fixes
typing-extensions>=4.8.0

# Optional: Upgrade to new langchain-ollama if available
# langchain-ollama>=0.1.0